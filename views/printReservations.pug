doctype html
html
  head
    title= title
    link(rel='stylesheet', href='/stylesheets/print.css')
  
  body
    - var counter = 0;
    - var rowsPerPage = 35;
    
    each val, index in data
      if (counter === 0)
        <div class="page">
          <table>
            <tr>
              <td
        - counter = 0;
      
      .page
        - var i = 1;
        table
          while i <= 37
            tr
              td= i
            - i++;
